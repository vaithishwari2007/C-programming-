#include <stdio.h>



int square(int n) {

    return n * n;

}

int main() {

    int n;

    scanf("%d", &n);

    if (n >= 1 && n <= 9) {

        int result = square(n);

        

        printf("Square of %d is %d", n, result);

    } else {

        printf("Invalid Input");

    }

    return 0;

}
